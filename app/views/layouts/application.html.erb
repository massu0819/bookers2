<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/c9c396223a.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Bookers</a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
           <ul class="navbar-nav ml-auto">
           <% if user_signed_in? %>
             <li class="nav-item">
                 <%= link_to 'Home', user_path(current_user.id), class:"nav-link active text-light fas fa-home" %>
             </li>
             <li class="nav-item">
                 <%= link_to 'Users', users_path, class:"nav-link active text-light fas fa-users" %>
             </li>
             <li class="nav-item">
                 <%= link_to "Books", books_path, class:"nav-link text-light fas fa-book-open" %>
             </li>
             <li class="nav-item">
                 <%= link_to "Log out", destroy_user_session_path, method: :delete, class:"nav-link text-light fas fa-sign-out-alt" %>
             </li>
           <% else %>
             <li class="nav-item">
                 <%= link_to "Home", root_path, class:"nav-link active text-light fas fa-home" %>
             </li>
             <li class="nav-item">
                 <%= link_to "About", home_about_path, class:"nav-link text-light fas fa-link" %>
             </li>
             <li class="nav-item">
                 <%= link_to "Sign up", new_user_registration_path, class:"nav-link text-light fas fa-user-plus" %>
             </li>
             <li class="nav-item">
                 <%= link_to "Log in", new_user_session_path, class:"nav-link text-light fas fa-sign-in-alt" %>
             </li>
           <% end %>
           </ul>
         </div>
       </div>
     </nav>
    </header>
    <%# byebug %>
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value) %>
    <% end %>
    <div class="text-center ml-4 mt-5">
      <% if user_signed_in? %>
        <%= render 'searchs/form' %>
      <% end %>
    </div>
    <%= yield %>
    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>