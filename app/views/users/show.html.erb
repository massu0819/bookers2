
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
      <table class="table">
        <thead>
          <tr> 
            <tr><tr width="120">name</tr><td><%= @user.name %></td></tr>
            
            <tr><tr>introduction</tr><td></td></tr>
          </tr>
          <tr>
            
            <td></td>
            <td></td>
          </tr>
        </thead>
        <tbody></tbody>


        <tbody>

        </tbody>

      </table>

    </div>
  </div>
</div>


<% if @user.id == current_user.id %>
  <p><%= link_to "プロフィール編集", edit_user_path(@user) %></p>
<% end %>